import '../../styles/sign-in/sign-in-password-reset.css';

const SignInMainPasswordReset = ({ onAnimation, onClosePasswordReset }) => {

    const animationStyles = onAnimation;

    return (
        <div className='sign-in-password-reset' style={animationStyles}>
            
        </div>
    );
}

export default SignInMainPasswordReset;